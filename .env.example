# Sanity (CMS) — leave blank to use built-in fallback content
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
# Server-side only — token for drafts/preview (not needed for read-only)
SANITY_API_TOKEN=
